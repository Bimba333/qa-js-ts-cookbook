# Практика: Literal Types

## Концептуальные вопросы

1. Чем `string` отличается от `'passed'`?
2. Почему литеральный тип работает только на этапе проверки?
3. Где литеральные типы полезны в QA-коде?
4. Почему литеральный тип не валидирует внешний JSON?
5. Когда литеральный тип может быть слишком узким?

## Чтение кода

Какое значение разрешено переменной?

```typescript
const environment: 'staging' = 'staging';
```

## Предскажите результат проверки

Будет ли TypeScript считать код согласованным?

```typescript
const status: 'passed' = 'failed';
```

## Задание на отладку

Найдите проблему.

```typescript
const retryCount: 3 = 2;
```

## Задание Automation QA

Опишите переменную `testStatus`, которая может иметь только значение `'passed'`.

## Мини-проект

Создайте функцию `printEnvironment`, которая принимает только значение `'staging'` и выводит его.
