# Практика: Пользовательские проверки и soft assertions

## 1. Граница custom matcher
Объясните, когда повторяющиеся `expect` образуют устойчивое предметное правило.

## 2. Предскажите результат
Первая `expect.soft()` не проходит, после неё устанавливается контрольный флаг, а вторая проверка проходит. Каким будет значение флага и итоговый статус теста?

## 3. Диагностика опасной мягкой проверки
После мягкой проверки наличия ID тест выполняет изменяющий запрос с этим ID. Найдите риск.

## 4. Реализация matcher
Создайте matcher для краткой модели задачи с ожидаемым, фактическим и понятным диагностическим сообщением. Добавьте отдельное сообщение для неуспешной проверки через `.not`.

## 5. Безопасное attachment
Определите, какие данные можно приложить и что требуется замаскировать.

## 6. Мини-проект: предметные проверки
Сравните три независимых поля через `expect.soft()`, целостное правило через matcher и приложите безопасный JSON. Объясните, почему мягкая ошибка не превращается в предупреждение.
