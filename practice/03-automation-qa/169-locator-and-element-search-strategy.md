# Практика: Locator и стратегия поиска элементов

## Задача 1. Концептуальные вопросы

Почему `Locator` называют ленивым запросом и чем это полезно при обновлении DOM?

## Задача 2. Выбор стратегии

Выберите `Locator` для кнопки `Оплатить`, поля с label `Email`, строки заказа `order-42` и технического canvas без доступной семантики. Обоснуйте решения.

## Задача 3. Предскажите результат

На странице две кнопки `Удалить`. Что произойдёт при `page.getByRole("button", { name: "Удалить" }).click()` и почему?

## Задача 4. Debugging

Замените хрупкий `Locator` `.orders > div:nth-child(4) > button` для строки с текстом `order-42` и кнопкой `Открыть`.

## Задача 5. Automation QA

Сравните поиск по role и test id для локализуемой кнопки. Назовите пользу и риск каждого.

## Задача 6. Мини-проект

Создайте локальный список из трёх заказов. Через `filter({ hasText })` найдите один заказ и проверьте его кнопку. Не используйте позиционный CSS.
