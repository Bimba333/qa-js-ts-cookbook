# Практика: Protocol Buffers: сервисы и сообщения

## Задача 1. Концептуальные вопросы

Что описывают `package`, `service`, `rpc`, message и номер поля? Почему тип TypeScript не заменяет protobuf-контракт?

## Задача 2. Анализ совместимости

Из `Task` удалили `string legacy_id = 8`, а затем номер 8 назначили полю `bool archived`. Объясните риск и исправьте контрактное решение.

## Задача 3. Предскажите результат

Новый server добавил `optional string external_reference = 9`, а старый client этого поля не знает. Какое поведение ожидается при совместимом развитии protobuf-контракта?

## Задача 4. Поиск ошибки

Разработчик изменил сгенерированный `Task.ts`, но не `.proto`. Почему изменение не обновляет protobuf-контракт?

## Задача 5. Automation QA

Составьте контрольный список проверки изменения `.proto`: новое поле, удалённое поле, смена типа поля и новый RPC method.

## Задача 6. Мини-проект

Создайте локальный `.proto` с service, unary method, request и response messages. Зарезервируйте один удалённый номер и объясните выбранные номера полей.
