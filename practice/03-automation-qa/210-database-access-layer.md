# Практика: Database Access Layer

## 1. Назначение слоя

Объясните, какую проблему решает DAL и почему универсальный `execute(sql, params)` не создаёт API предметной области.

## 2. Предскажите результат

Значение `numeric` равное `2.50` возвращено как строка, а транспортная модель содержит `number` `2.5`. Почему строгое сравнение не пройдёт?

## 3. Отладка преобразования

Исправьте преобразование, которое заменяет `NULL` из базы данных отсутствующим свойством без контрактного решения.

## 4. Реализация метода репозитория

Напишите `findTask(id)`, использующий параметризованный запрос и возвращающий преобразованную запись или `undefined`.

## 5. Граница проверок

Объясните, почему repository не должен содержать Playwright `expect()`.

## 6. Мини-проект: TasksRepository

Спроектируйте узкий репозиторий с `createTask`, `findTask` и `deleteTask`, сохранив владение подключением снаружи.
