# Практика: Нормализация и проверки между слоями

## 1. Каноническая модель
Выберите бизнес-поля задачи для конкретного сравнения UI, API, gRPC и PostgreSQL. Объясните, почему эта модель не обязана становиться общей моделью приложения.

## 2. Предскажите результат
Сравниваются `"2.50"` из PostgreSQL и `2.5` из API без нормализации. Почему проверка даст расхождение?

## 3. Диагностика опасной нормализации
Adapter заменяет любое некорректное число на `0`. Какой дефект он маскирует?

## 4. Реализация преобразования
Преобразуйте `numeric`, timestamp и owner в проверенную каноническую модель. Сохраните отсутствующее значение, `null` и пустую строку как разные маркированные состояния.

## 5. Владение полями
Объясните, почему техническая колонка базы данных не обязана входить в сравнение с API.

## 6. Мини-проект: сравнение между слоями
Создайте отдельные adapters для API и PostgreSQL, явно преобразуйте их статусы, нормализуйте даты, числа и необязательные значения, затем сравните канонические модели.
