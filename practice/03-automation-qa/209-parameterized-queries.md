# Практика: Parameterized queries

## 1. Текст SQL и значения

Объясните, почему позиционные параметры защищают значения, но не делают безопасными динамические имена схем, таблиц или колонок.

## 2. Предскажите результат

Что произойдёт с SQL, если заголовок `QA engineer's task` вставить конкатенацией? Как параметризованный запрос меняет обработку?

## 3. Отладка NULL

Исправьте `SELECT id FROM tasks WHERE description = NULL`.

## 4. Реализация INSERT RETURNING

Напишите parameterized `INSERT` с `RETURNING id, description` и проверьте `rowCount`.

## 5. Детерминированный SELECT

Исправьте проверку массива строк, которая полагается на порядок без `ORDER BY`.

## 6. Мини-проект: безопасная функция запросов

Реализуйте узкую функцию поиска задачи по ID без конкатенации SQL и с явным `undefined` при отсутствии строки.
