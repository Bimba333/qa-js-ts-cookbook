# Практика: Архитектурные слои и поток зависимостей

## Задача 1. Концептуальные вопросы

1. Что делает границу слоя публичной?
2. Почему циклическая зависимость затрудняет изменение?
3. Почему проекту не обязательно иметь все возможные слои?

## Задача 2. Чтение потока зависимостей

Тесты используют `OrdersApi` и `OrdersDatabase`. `OrdersApi` импортирует конфигурацию. `OrdersDatabase` импортирует конфигурацию. Есть ли здесь цикл? Кто должен оркестрировать подготовку через API и проверку через базу данных?

## Задача 3. Предскажите результат выполнения кода

```ts
type Reader = { read(): string };
const reader: Reader = { read: () => "created" };
console.log(reader.read());
```

Что будет выведено? Какая часть является публичной границей?

## Задача 4. Найдите нарушение

UI-модуль импортирует файл теста, чтобы получить тестовые данные. Файл теста импортирует UI-модуль для действий. Объясните проблему и перенесите владение данными в подходящую границу.

## Задача 5. Automation QA

Спроектируйте направление зависимостей для тестов, API-клиента, слоя доступа к базе данных, конфигурации и диагностики. Укажите, какие зависимости запрещены.

## Задача 6. Мини-проект

Предложите минимальные слои для проекта, который проверяет REST API и PostgreSQL, но не имеет UI и gRPC. Опишите публичный API каждого слоя и точку сборки зависимостей (Composition Root) без контейнера внедрения зависимостей.
