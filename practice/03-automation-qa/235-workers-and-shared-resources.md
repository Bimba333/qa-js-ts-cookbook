# Практика: Workers и shared resources

## 1. Worker и project
Объясните различие процесса worker и Playwright project.

## 2. Замена worker
Что происходит с локальным состоянием процесса после падения и замены worker?

## 3. Общая учётная запись
Спроектируйте разделение учётных записей по идентификатору выполнения и точную очистку.

## 4. Идентификаторы worker
Объясните роли `workerIndex` и `parallelIndex` без обещания глобальной стабильности.

## 5. Automation QA-задача
Устраните конфликт двух тестов, создающих одну запись базы данных; `workers: 1` не считается исправлением.

## 6. Мини-проект: идентификатор владельца ресурса
Постройте ограниченный идентификатор из project, shard, worker, parallel, repeat, retry и полного `titlePath`. Сделайте его безопасным для имени файла и устойчивым к одинаковым коротким названиям тестов.
