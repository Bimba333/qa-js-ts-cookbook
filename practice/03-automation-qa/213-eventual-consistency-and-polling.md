# Практика: Eventual consistency и polling

## 1. Ожидание по условию

Объясните, почему polling ожидает выполнения условия, а не истечения фиксированного времени.

## 2. Предскажите результат

Строка появляется через 80 ms, а пауза равна 50 ms. Что увидит однократный `SELECT` и почему увеличение паузы не является надёжным решением?

## 3. Отладка бесконечного polling

Добавьте предельный срок, интервал и диагностическое сообщение о тайм-ауте в цикл без границы завершения.

## 4. Реализация polling

Реализуйте ожидание конкретной задачи по ID с параметризованным `SELECT` и диагностикой последнего прочитанного значения.

## 5. Ошибка подключения

Объясните, почему ошибку подключения нельзя трактовать как «строка ещё не появилась».

## 6. Мини-проект: отложенное сохранение

Создайте контролируемый источник данных, дождитесь строки с помощью ограниченного polling и гарантированно завершите источник и работу с базой данных.
