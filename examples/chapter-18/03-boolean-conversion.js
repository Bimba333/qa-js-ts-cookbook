const filledText = 'false';
const emptyText = '';
const retryCount = 0;
const userName = 'Anna';

console.log(Boolean(filledText));
console.log(Boolean(emptyText));
console.log(Boolean(retryCount));
console.log(Boolean(userName));
