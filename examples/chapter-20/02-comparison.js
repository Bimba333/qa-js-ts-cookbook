const expectedStatusCode = 200;
const actualStatusCode = 200;
const responseTimeMs = 350;

console.log(actualStatusCode === expectedStatusCode);
console.log(responseTimeMs < 500);
console.log(responseTimeMs >= 100);
