const itemPrice = 100;
const itemCount = 3;
const discount = 50;

const totalBeforeDiscount = itemPrice * itemCount;
const totalAfterDiscount = totalBeforeDiscount - discount;

console.log(totalBeforeDiscount);
console.log(totalAfterDiscount);
