const retryCountFromEnv = '3';
const nextRetryCount = retryCountFromEnv + 1;
const arithmeticResult = 2 + 3 * 4;
const explicitResult = (2 + 3) * 4;

console.log(nextRetryCount);
console.log(arithmeticResult);
console.log(explicitResult);
