const testUsers = [
  'anna@example.test',
  'kate@example.test',
  'max@example.test'
];

const lastIndex = testUsers.length - 1;

console.log(testUsers.length);
console.log(lastIndex);
console.log(testUsers[lastIndex]);
