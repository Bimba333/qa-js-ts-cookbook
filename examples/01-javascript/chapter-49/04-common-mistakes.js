const testRun = ['login smoke', 'create order', 'pay order'];

for (const testCase of testRun) {
  console.log(`Current test case: ${testCase}`);
}

console.log('Use for...of when you need the element itself.');
